/**
 * The xdg-shell protocol: desktop window management (toplevels, popups, the configure/ack
 * lifecycle). The classes in this package are <em>generated at build time</em> by
 * {@code wayland-scanner} from the vendored {@code xdg-shell.xml}; only this file is
 * hand-written. Cross-protocol references resolve to
 * {@link io.github.nhwalker.wayland.client}.
 */
package io.github.nhwalker.wayland.protocols.xdg;
